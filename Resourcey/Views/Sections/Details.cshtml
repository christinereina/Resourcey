@{
  Layout = "_Layout";
}

@model Resourcey.Models.Section

<h1>Section Details</h1>
<hr/>

<h2>@Html.DisplayFor(model => model.Name)</h2>

@if(@Model.Resources.Count == 0)
{
  <p>There are no resources yet.</p>
}
else
{
  <h3>Resources in this section:</h3>
  <ul>
    @foreach (var resource in Model.Resources)
    {
      <p>@Html.ActionLink($"{resource.Name}", "Details", "Resources", new { id = resource.ResourceId } )</p>
    }
  </ul>
}

<button type="button" class="btn btn-warning">@Html.ActionLink("Create a resource", "Create", "Resources")</button>

<p>@Html.ActionLink("Edit section", "Edit", new { id = Model.SectionId })</p>
<p>@Html.ActionLink("Delete section", "Delete", new { id = Model.SectionId })</p>
